import type { Page } from "@dans-framework/pages";

const page: Page = {
  id: "dashboard",
  name: "Dashboard",
  slug: "/",
  template: "dashboard",
  inMenu: true,
  menuTitle: {
    en: "Dashboard",
    nl: "Dashboard",
  },
};

export default page;
