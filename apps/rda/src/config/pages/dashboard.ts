import type { Page } from "@dans-framework/pages";

const page: Page = {
  id: "dashboard",
  name: "Dashboard",
  slug: "dashboard",
  template: "dashboard",
  inMenu: true,
  menuTitle: {
    en: "Dashboard",
    nl: "Dashboard",
  },
};

export default page;
