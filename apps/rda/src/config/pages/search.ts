import type { Page } from "@dans-framework/pages";

const page: Page = {
  id: "search",
  name: {
    en: "Search RDA",
    nl: "Zoek RDA",
  },
  slug: "search",
  template: "search",
  inMenu: true,
  menuTitle: {
    en: "Search",
    nl: "Search",
  },
};

export default page;
