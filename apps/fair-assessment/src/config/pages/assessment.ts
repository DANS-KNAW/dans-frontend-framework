import type { Page } from "@dans-framework/pages";

const page: Page = {
  id: "assessments",
  name: "Assessments",
  slug: "assessments",
  template: "assessments",
  inMenu: true,
  menuTitle: {
    en: "Assessments",
    nl: "Beoordelingen",
  },
};

export default page;
