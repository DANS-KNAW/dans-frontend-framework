import type { Page } from "@dans-framework/pages";

const page: Page = {
  id: "create",
  name: "Create Assessment",
  slug: "create",
  inMenu: true,
  menuTitle: {
    en: "Create assessment",
    nl: "Maak beoordeling",
  },
};

export default page;
