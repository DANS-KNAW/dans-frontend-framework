import type { Page } from "@dans-framework/pages";

const page: Page = {
  id: "deposit",
  name: "Deposit",
  slug: "deposit",
  template: "deposit",
  inMenu: true,
  menuTitle: {
    en: "Deposit",
    nl: "Deponeren",
  },
};

export default page;
