import type { Page } from "@dans-framework/pages";

const page: Page = {
  id: "search",
  name: "Search",
  slug: "/search",
  template: "search",
  inMenu: false,
};

export default page;
