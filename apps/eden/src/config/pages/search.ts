import type { Page } from "@dans-framework/pages";

const page: Page = {
  id: "search",
  name: "Search",
  slug: "/",
  template: "search",
  inMenu: true,
  menuTitle: {
    en: "Search",
    nl: "Zoeken",
  },
};

export default page;
