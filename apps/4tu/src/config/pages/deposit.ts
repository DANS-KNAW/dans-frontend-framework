import type { Page } from "@dans-framework/pages";

const page: Page = {
  id: "deposit",
  name: "Deposit",
  slug: "deposit",
  template: "deposit",
  inMenu: false,
  menuTitle: "Deposit",
};

export default page;
